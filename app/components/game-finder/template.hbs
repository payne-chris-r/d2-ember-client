<div class="filter-row">
  <div class="collapse navbar-collapse">
    <ul class="nav navbar-nav">
      {{#if isAuthenticated}}
      <li>MMR Filter:</li>
      {{/if}}
    </ul>
    <ul class="nav navbar-nav">
      {{#if isAuthenticated}}
        Language Filter:
      {{/if}}
    </ul>
    <ul class="nav navbar-nav">
      {{#if isAuthenticated}}
        Position Filter:
      {{/if}}
    </ul>
    <ul class="nav navbar-nav navbar-right">
      <li>{{#link-to "sign-in"}}Submit{{/link-to}}</li>
    </ul>
  </div>
</div>
