Team:
{{#each model.profiles as |profile|}}
<div>
  Player:
  {{profile.id}}
  {{profile.first_name}}
  {{profile.last_name}}
</div>
{{/each}}
{{#if model.open}}
  <span class='open'>Game is OPEN</span>
  {{else}}
  <span class='closed'>There are no open spaces in this lobby.</span>
{{/if}}
{{#link-to 'games'}}
  <button type="button">Back to All Games</button>
{{/link-to}}
