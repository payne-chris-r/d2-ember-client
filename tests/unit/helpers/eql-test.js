import { eql } from 'dota2-ember-client/helpers/eql';
import { module, test } from 'qunit';

module('Unit | Helper | eql');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = eql([42]);
  assert.ok(result);
});
