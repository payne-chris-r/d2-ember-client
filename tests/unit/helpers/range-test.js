import { range } from 'dota2-ember-client/helpers/range';
import { module, test } from 'qunit';

module('Unit | Helper | range');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = range([42]);
  assert.ok(result);
});
